{
  "name": "style-transform",
  "version": "0.1.4",
  "description":
    "A toolkit for fast and flexible manipulation of css-in-js styles",
  "author": "Jon Whitehead",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prettier":
      "prettier --single-quote --trailing-comma all --write \"{src,test}/**/*.{ts,tsx}\"",
    "clean": "rimraf lib/*",
    "build": "yarn run clean && tsc",
    "test": "jest",
    "prepare": "yarn run test && yarn run build"
  },
  "jest": {
    "transform": {
      ".(ts|tsx)": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(ts|tsx|js)$",
    "moduleFileExtensions": ["ts", "tsx", "js"]
  },
  "devDependencies": {
    "@types/jest": "^20.0.8",
    "jest": "^21.0.1",
    "prettier": "^1.6.1",
    "rimraf": "^2.6.1",
    "ts-jest": "^21.0.0",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "css-shorthand-expand": "^1.1.0",
    "object-path-immutable": "^0.5.1"
  }
}
